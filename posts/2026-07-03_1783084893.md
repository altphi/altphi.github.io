
TIL about `taskset` from _util linux_ which lets one run a thing and isolate it
to a specific CPU/core.  This allows for more consistent speed testing.

`taskset -c 0 ./my-app`   # this runs on cpu #0 only

#tech
