

Uses of `Box`:

1. For recursive types:  Since their size cannot be known at compile time, rust uses a `Box` which is a pointer on the _stack_ to contents on the _heap_.

```rust
enum List {
  Cons(i16, Box<List>),
  Nil,
}
```

2. Data larger than the stack (staci is typically up to ~8MB).

3. Trait objects

#rust

#dailies
