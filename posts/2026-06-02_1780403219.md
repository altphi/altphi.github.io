
Refs for tree nodes in rust...

```rust
let tree: Option<Box<Node>> = Some(...);

let a = &tree;
// &Option<Box<Node>>

let b = tree.as_ref();
// Option<&Box<Node>>

let c = tree.as_deref();
// Option<&Node>
```


#tech
