

A _tangent line_ passes through a given point and shares the same slope of the
curve at that point.

A _normal line_ passes through a given point and has perpendicular slope to the slope of the
curve at that point.   So if the tangent line's slope is _m_ then the normal
line's slope is $$ -\frac{1}{m} $$.

#math
